# Default variables for all environments
# These can be overridden in environment-specific configs or in databricks.yml targets

variables:
  # Unity Catalog configuration
  catalog_name:
    description: "Unity Catalog name for the environment"
    default: ab_dev_catalog

  # Notification settings
  email_notifications:
    description: "Email address for job notifications"
    default: pandi.anbu@zeb.co

  # Git configuration
  git_branch:
    description: "Git branch name"
    default: dev

  # Workspace configuration
  workspace_host:
    description: "Databricks workspace URL"
    default: https://adb-201068313543333.13.azuredatabricks.net

  # Service Principal configuration
  service_principal_id:
    description: "Service Principal ID for run_as"
    default: a7923e63-e14c-4c32-87cc-e89e3b4a165a

