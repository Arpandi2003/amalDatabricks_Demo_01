# DEV Environment Configuration
# Override default variables for DEV environment

variables:
  # Unity Catalog configuration
  catalog_name:
    default: ab_dev_catalog
  
  # Notification settings
  email_notifications:
    default: pandi.anbu@zeb.co
  
  # Git configuration
  git_branch:
    default: dev
  
  # Workspace configuration (update with actual DEV workspace URL if different)
  workspace_host:
    default: https://adb-201068313543333.13.azuredatabricks.net
  
  # Service Principal configuration (update with DEV-specific SP if available)
  service_principal_id:
    default: a7923e63-e14c-4c32-87cc-e89e3b4a165a
  
  # Environment-specific settings
  environment_name:
    default: Development
  
  # Storage configuration
  storage_account:
    default: stgazdbricksdev001
  
  external_location_container:
    default: dev-external-location

